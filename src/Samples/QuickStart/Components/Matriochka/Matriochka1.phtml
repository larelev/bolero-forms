<?php

namespace Appliance;

use function Bolero\Forms\Hooks\useEffect;

function Matriochka1($children)
{
    useEffect(function ($children, /* string */ $id, /* string */ $label) {
        $id = $children->props()->id;
        $label = $children->props()->label;
    });

    return (<<< HTML
    <label for="{{ id }}">{{ label }}</label>
    <ul id="{{ id }}">
        <li>
        {{ children }}
        </li>
    </ul>
    HTML);
}